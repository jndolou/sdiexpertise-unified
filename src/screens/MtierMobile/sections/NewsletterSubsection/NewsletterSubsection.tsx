import React from "react";
import { NewsletterSection } from "../../../../components/shared";

export const NewsletterSubsection = (): JSX.Element => {
  return <NewsletterSection useIconButton={false} />;
};
