import React from "react";
import { NewsletterSection as SharedNewsletterSection } from "../../../../components/shared";

export const NewsletterSection = (): JSX.Element => {
  return <SharedNewsletterSection useIconButton={false} />;
};
